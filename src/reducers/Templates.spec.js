import chai, {expect} from 'chai';
import spies from 'chai-spies';

chai.use(spies);


describe('Templates reducer', () => {
  describe('TEMPLATE_ADD', () => {
    it('should return the initial state');

  });

  describe('TEMPLATE_UPDATE', () => {
    it('should return the initial state');
  });
});